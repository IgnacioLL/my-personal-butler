# Calendar fixtures

Busy weeks, conflicts, and timezone edge cases for in-memory calendar harness tests.

| File | Purpose |
| --- | --- |
| `busy-friday.json` | Seed events overlapping a Friday morning focus block (conflict + free-slot tests) |

See [`agent-plan/capabilities/calendar.md`](../../agent-plan/capabilities/calendar.md).
