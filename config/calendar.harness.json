{
  "skill": "calendar",
  "mode": "memory",
  "provider": "in_memory",
  "live": false,
  "notes": "Harness/CI default. StubCalendarAdapter + CalendarStore; soft confirm via ActionGateway (INV-APPR-003). Never set CALENDAR_MODE=google or CALENDAR_LIVE in CI."
}
