# Scripts

| Script | Purpose |
| --- | --- |
| [`test-ci.sh`](./test-ci.sh) | **`test:ci`** entrypoint — T0 stub; TASK-01 adds unit/contract/integration + INV-* runner |

Run from repo root:

```bash
./scripts/test-ci.sh
make test-ci
```
