"""Android companion node doubles — import submodules directly.

Use:
  channels.android.projection
  channels.android.approvals
  channels.android.status
  channels.android.notifications

Production pairing (live OpenClaw Android node): docs/android-pairing.md
CI keeps these API doubles (config/android.harness.json).
"""
